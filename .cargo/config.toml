# Fix for MSYS2/MinGW GNU linker "export ordinal too large" error.
# Uses lld linker which has no export limit.
#
# Prerequisites (run in MSYS2 terminal):
#   pacman -S mingw-w64-ucrt-x86_64-lld
#
[target.x86_64-pc-windows-gnu]
rustflags = ["-C", "link-arg=-fuse-ld=lld"]
